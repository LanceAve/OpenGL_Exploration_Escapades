#version 330 // OpenGL version 3.30 (modified the requirement)

in vec3 retNormal;				// replacing color to normal
in vec2 retTexCoord; 

out vec4 FragColor;		
uniform sampler2D sampler1;
uniform sampler2D sampler2;

void main() 
{
	// will only load texture, with no color info (for now)
	FragColor = texture(sampler1, retTexCoord);				// Example 1 - with textures only
}