#version 330 // OpenGL version 3.30 (modified the requirement)

in vec4 retColor;		// MUST use the name as the out variable that's used by the vertex shader
out vec4 FragColor;		

void main() 
{
	FragColor = retColor;
	// Removing the color selection buffer (remove tool window later)
}